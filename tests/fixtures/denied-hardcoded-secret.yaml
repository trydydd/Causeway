# yaml-language-server: $schema=https://causeway.internal/schema/manifest/v1
# DENIED: hardcoded secret in envVars — policy violation, never auto-retried
apiVersion: causeway/v1
name: shift-planner
description: >
  Generates a daily shift schedule for the warehouse team based on staff
  availability. Used by one team lead to plan their own team's shifts.
runtime: python3.12
entrypoint: python app.py
port: 8080
healthEndpoint: /health
tier: personal-team
dataSources: []
envVars:
  - name: DATABASE_URL
    description: postgres://admin:s3cr3tpassword@db.internal/shifts
    required: true
