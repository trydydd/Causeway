[pytest]
# Integration tests build and run real containers. They are opt-in everywhere so
# the default suite (and CI) stays deterministic and runtime-free. Run them with:
#   pytest -m integration
addopts = -m "not integration"
markers =
    integration: requires a running container runtime (docker/nerdctl) and network
